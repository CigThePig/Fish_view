name: Temporary branch verification

on:
  push:
    branches: [fix/environment-boundaries-and-plant-anchoring]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run test suite
        run: node --test
