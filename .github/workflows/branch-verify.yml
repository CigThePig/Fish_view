name: Verify final fish body profiles

on:
  push:
    branches: [feat/promote-final-fish-body-profiles]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Run test suite
        run: node --test
